---
plan: 10-02
phase: 10
wave: 1
title: "Add resolved_csproj_paths field to MigrationState"
depends_on: []
files_modified:
  - app/core/state.py
autonomous: true
requirements_addressed: [ADV-01]
---

## Objective

Extend `MigrationState` with `resolved_csproj_paths: list[str]` to persist the output of `resolve_from_entry()` into the shared LangGraph state. This field is consumed by the `migrate_task_node` pre-flight and the `validator_node`.

---

## Tasks

### Task 1: Add `resolved_csproj_paths` to `MigrationState`

<read_first>
- app/core/state.py (full file — read the complete current MigrationState before editing)
</read_first>

<action>
In `app/core/state.py`, add the following field to `MigrationState` after the `worktree_path` field (line 72):

```python
    resolved_csproj_paths: list[str] = Field(default_factory=list)
```

The field must be inserted between `worktree_path` and `session_id` so that related fields stay grouped. Do not move any existing fields. The result should look like:

```python
    worktree_path: str | None = None
    resolved_csproj_paths: list[str] = Field(default_factory=list)
    session_id: str = ""
```
</action>

<acceptance_criteria>
- `grep "resolved_csproj_paths" app/core/state.py` returns a match
- `python -c "from app.core.state import MigrationState; s = MigrationState(migration_id='x', solution_path='/tmp'); print(s.resolved_csproj_paths)"` outputs `[]` and exits 0
- `grep "worktree_path" app/core/state.py` still present (no regression)
</acceptance_criteria>

---

## Verification

```bash
grep -n "resolved_csproj_paths" app/core/state.py
python -c "from app.core.state import MigrationState; s = MigrationState(migration_id='x', solution_path='/tmp'); print(type(s.resolved_csproj_paths))"
```

## must_haves

- [ ] `resolved_csproj_paths: list[str]` field present in `MigrationState`
- [ ] Default is `list` (not `None`) so callers don't need null checks
- [ ] Pydantic model still validates without errors
